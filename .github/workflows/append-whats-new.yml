name: Append whats_new rows

on:
  workflow_dispatch:
    inputs:
      payload_b64:
        description: 'Base64-encoded JSON payload'
        required: true
        type: string

jobs:
  append:
    runs-on: ubuntu-latest
    steps:
      - name: Decode payload and POST to Apps Script
        env:
          PAYLOAD_B64: ${{ inputs.payload_b64 }}
        run: |
          printf '%s' "$PAYLOAD_B64" | base64 -d > /tmp/payload.json
          echo "Payload:"
          cat /tmp/payload.json
          RESPONSE=$(curl -s -L -X POST \
            "https://script.google.com/macros/s/AKfycbyn23sj7EbwNfzKDB8kP9vhB_-lWBNUiHzZ70u7dBncHn2iNQRuxuwo_XE8iLWNQwOp/exec" \
            -H "Content-Type: application/json" \
            -d @/tmp/payload.json)
          echo "Apps Script response: $RESPONSE"
          if echo "$RESPONSE" | grep -q '"success":true'; then
            echo "Success"
          else
            echo "ERROR: Apps Script did not return success: $RESPONSE"
            exit 1
          fi
