User-agent: *
Allow: /

# The two Disallow lines that used to sit here blocked
# /AI-Use-Policy-Template.docx and .pdf. They existed to stop search engines
# indexing the files so nobody could reach the download without passing the
# Substack capture. The gate was dropped on 30 Aug 2026 (reasoning in
# SCRATCHPAD), so the rule they enforced no longer exists, and blocking a file
# the site now links from an indexable page as its primary call to action would
# be self-contradictory.

Sitemap: https://theeditai.co.uk/sitemap.xml
