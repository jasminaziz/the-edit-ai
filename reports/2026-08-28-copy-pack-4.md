# Copy pack four: the positioning pass strings

**Approved by Jasmin, 28 August 2026 (Cowork positioning thread; items 1 to 3 revised and re-approved the same day for the empowerment framing and Jasmin's own register).** These are
exact strings. Code sessions place them verbatim: no rewording, no added
punctuation, no em dashes introduced anywhere, no improvised variants for
states not listed here. Source: the surface audit
(`reports/2026-08-28-surface-audit.md`) against the signed positioning
statement.

---

## 1. /tools subheading

Replaces the CobaltZone subheading in `src/pages/Tools.tsx`.

```
Pick the tool for the job. The checks give you a head start; the final call is yours.
```

## 2. Homepage intro paragraph

Replaces the second paragraph of the intro block in `src/pages/Index.tsx`.
The lead line "There's a lot to keep up with. This helps." is unchanged and
must not be touched.

```
An opinionated directory of AI tools for communications teams in charities, cultural organisations and heritage. What each tool is, what it's for, what it really costs, and what to mind when you use it. Every one has been through the checks before it appears here, so you're starting informed instead of from scratch. No sponsored listings, no affiliate links.
```

## 3. About panel premise paragraph

Replaces the body paragraph in `src/components/AboutPanel.tsx`. This is the
canonical premise; the JSON-LD description on the homepage should be kept
consistent with its first sentence.

```
The Edit is my opinionated directory of AI tools for comms teams in charities, cultural organisations and heritage. I built it because the lists out there answer none of the questions this sector actually has to ask: where your data sits, whether the tool trains on what you type into it, whether there's a charity price, whether you could explain it to your board in one sentence. So I check. Every tool here has been through those checks before it appears, the ones that failed are published too, and the final call stays yours. No sponsored listings, no affiliate links, nobody paying to be recommended. I'm Jasmin, a strategic communications consultant who works with exactly these teams. This is the resource I wanted to hand people, so I made it.
```

## 4. Homepage counter

Heading (uppercase treatment unchanged):

```
Through the checks
```

Caption:

```
tools that have been through the checks
```

## 5. Metas

Homepage description (title unchanged):

```
An opinionated AI tools directory for charity, cultural and heritage comms teams. What each tool is for, what it really costs, and the data and training checks already done. No sponsored lists.
```

/tools description (title unchanged):

```
Curated AI tools for charity, cultural and heritage communications. What each one is for and what it costs, with the data, training and DPIA checks already done on every card.
```

/learning description (title unchanged):

```
Start here if AI still feels like someone else's subject. Learning resources chosen for charity, cultural and heritage comms teams.
```

/my-stack title (description unchanged; see item 12):

```
My AI Stack | The Edit
```

## 6. Failure-row badge

**Mechanism ruled 28 Aug: a `not_recommended` status value in column C.**
Jasmin sets it on the three failure rows (Seedance, DeepSeek, Grok, per the
A3 sheet); the badge renders uppercase like IN MY STACK, source stays
sentence case:

```
Judged, not recommended
```

## 7. My Stack framing line

New line under the CobaltZone subheading on `src/pages/MyStack.tsx`:

```
The directory is what I'd recommend for your organisation. This is everything I run myself, including the tools that build this site.
```

## 8. /submit subheading

Replaces the CobaltZone subheading in `src/pages/Submit.tsx`:

```
Spotted something worth putting through the checks? Tell me about it.
```

## 9. /ai-news show-more control

Replaces "Show more — {hiddenCount} remaining" in `src/pages/WhatsNew.tsx`
(this removes the branch's last visitor-facing em dash):

```
Show {hiddenCount} more
```

## 10. /policy-template, three strings

(a) Phrase fix inside the intro paragraph. Replace
"How to work out whether a DPIA is needed." with:

```
How to work out whether your organisation needs to do a DPIA.
```

(b) New ungated explainer block, placed on the page below the intro.
Heading:

```
What's a DPIA, and why does everyone keep saying it?
```

Body:

```
A Data Protection Impact Assessment is the structured look your organisation takes before using personal data in a way that could put people at risk. It's something you do, not something a tool has. It matters here because supporter and beneficiary data is exactly the kind that raises the stakes, and because your board will ask. The template walks you through when one's needed and what to write down.
```

(c) Board-reference line, appended to the end of the intro paragraph:

```
It's the document you can put in front of your board.
```

## 11. /learning subheading

Replaces the CobaltZone subheading in `src/pages/Learning.tsx`:

```
How I'm staying sharp, and where to start if you're new to all this.
```

## 12. The word "stack", ruled

Nav label, page name and page heading stay `My Stack`. The strip heading
`What I'm running` stays. Only two changes: the meta title in item 5, and
the homepage strip link, which becomes:

```
See everything I use →
```

## 13. /design-kit re-point (ruled 28 Aug: kept, not parked)

Meta description (title unchanged):

```
A design inventory for charity, cultural and heritage teams making their own things with AI. Keep your brand and your taste intact, from first reference to final check.
```

Framing line, new, under the CobaltZone subheading (subheading "From blank
page to build-ready." is unchanged):

```
The tools I use to keep taste in the process, in the order you'd actually use them. For teams making their own things with AI and wanting them to still look like your organisation.
```

Phase explainer edit, "Present the Work" only (the PHASES array in
`src/pages/DesignKit.tsx`; the other five explainers are unchanged):

```
Show the work properly. Device frames and scene mockups turn screenshots into something you can put in front of trustees and funders.
```

---

## Ruled 28 Aug, code jobs rather than copy

- **Checks-zone reorder:** the DPIA chip renders first in the card's checks
  zone, the two fact lines follow as its evidence. No copy changes.
- **Failure badge mechanism:** `not_recommended` as a column C status value;
  badge string in item 6. Suggested treatment, Jasmin may veto: solid badge,
  `#FFFFFF` text on `#A8261C` (the locked Red ink hex, ~7:1 so AA-safe),
  the IN MY STACK pattern. Distinct from the tinted DPIA chip by being
  solid.

## Still open, not in this pack

- Legal page rewrites: content Jasmin signs, drafted separately, not
  copy-pack strings.

---

*Approved 28 August 2026. Live until placed, then historical. Companion
files: the surface audit and the positioning statement, both 2026-08-28.*
