# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
# Claude Code's per-machine permissions. *.local above does not match it, and
# the global git ignore that does is not read by every tool (the Cowork bridge).
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.local
.env.*.local
.vercel

# Brainstorming companion mockups (local, not shipped)
.superpowers/
